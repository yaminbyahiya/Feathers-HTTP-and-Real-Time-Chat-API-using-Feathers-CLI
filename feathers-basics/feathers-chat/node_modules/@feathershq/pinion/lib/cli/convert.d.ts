import { PinionContext } from '../core';
export interface ConverterContext extends PinionContext {
    file: string;
    to: string;
}
export declare const convert: (ctx: ConverterContext) => Promise<void>;
