import { Callable, PinionContext } from '../core';
export declare const install: <C extends PinionContext>(dependencies: Callable<string[], C>, dev?: boolean, packager?: Callable<string, C>) => <T extends C>(ctx: T) => Promise<T>;
