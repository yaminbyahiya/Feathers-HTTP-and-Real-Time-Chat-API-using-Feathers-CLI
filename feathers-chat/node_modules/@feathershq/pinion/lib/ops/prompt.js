"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prompt = void 0;
const core_1 = require("../core");
const helpers_1 = require("./helpers");
/**
 * Show prompts using Inquirer
 *
 * @param prompts The prompt questions to ask
 * @returns The generator context updated with the prompt results
 */
const prompt = (prompts) => async (ctx) => {
    const answers = await ctx.pinion.prompt(await (0, core_1.getCallable)(prompts, ctx));
    const result = {
        ...ctx,
        ...answers
    };
    return (0, helpers_1.addTrace)(result, 'prompt', answers);
};
exports.prompt = prompt;
