import { QuestionCollection } from 'inquirer';
import { PinionContext, Callable } from '../core';
/**
 * Show prompts using Inquirer
 *
 * @param prompts The prompt questions to ask
 * @returns The generator context updated with the prompt results
 */
export declare const prompt: <C extends PinionContext, R extends PinionContext = C>(prompts: Callable<QuestionCollection<import("inquirer").Answers>, C>) => <T extends C>(ctx: T) => Promise<R>;
